//let produto1 = [560, 360, 380, 0];
//let produto2 = [340, 450, 420, 80];
//let produto3 = [280, 270, 210, 380];

//console.table(produto1);
//console.table(produto2);
//console.table(produto3);

let produto1 = [560, 360, 380, 0];
let produto2 = [340, 450, 420, 80];
let produto3 = [280, 270, 210, 380];

/*
let matriz = [
    [560, 360, 380, 0],
    [340, 450, 420, 80],
    [280, 270, 210, 380]
];


// Função para exibir a matriz em formato de tabela
function exibirMatriz(matriz) {
    for (let i = 0; i < matriz.length; i++) { //iterando entre cada linha
        let linha = ''; //variável vazia onde as linhas serão colocadas
        for (let j = 0; j < matriz[i].length; j++) { //iterando entre as colunas
            linha += matriz[i][j] +  ' '; // Adiciona um espaço simples para espaçamento e a váriavel linha
        }
        
        console.table({ 
            
        })
        //console.table(`produto[${i + 1}]: ${linha.trim()}`); // Exibe a linha
        }
        
    }

// Chamando a função para exibir a matriz
exibirMatriz(matriz);


*/
