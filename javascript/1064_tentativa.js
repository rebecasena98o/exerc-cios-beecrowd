var input = require('fs').readFileSync('Data/1064.txt', 'utf8');
var lines = input.split('\r\n');

//Criando as variáveis de soma e contador
let contadorPositivos = 0; 
let somaPositivos = 0;

//Criando o for para ele pegar o dado e a posição, dps verificar a condição
for(let i = 0; i < lines.length; i++){
    valor = parseFloat(lines[i]);
}

console.log("valor")